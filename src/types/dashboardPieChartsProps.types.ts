export interface DashboardPieChartsProps {
  stateData: Array<{ name: string; value: number }>;
  cultureData: Array<{ name: string; value: number }>;
  landUsageData: Array<{ name: string; value: number }>;
}
